// Plugins are declared per-module (see app/build.gradle.kts and core/build.gradle.kts)
// so that the pure-Kotlin :core module can be built and tested without the Android
// toolchain:  gradle -c settings-core.gradle.kts :core:test
